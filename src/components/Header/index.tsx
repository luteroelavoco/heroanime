import React from 'react';
import Nav from './Nav';
import { Container } from './styles';

const Header: React.FC = () => {
  return (
  <Container>
    <Nav />
  </Container>);
}

export default Header;